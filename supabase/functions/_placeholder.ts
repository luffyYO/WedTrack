// This file exists solely to satisfy the TypeScript compiler's requirement
// that the tsconfig "files" list is non-empty. The real Deno source files in
// this directory are handled by the Deno language server (vscode-deno), NOT
// the TypeScript language server. Do not import or use this file.
export {};
